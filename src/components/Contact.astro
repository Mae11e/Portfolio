<section id="contact" class="contact">
  <h2 class="section-title">Contact</h2>
  <p class="contact-text">
    Feel free to reach out — I'm happy to talk about data, projects, or opportunities.
  </p>
  <a href="mailto:maelle.mohr@epitech.eu" class="contact-email">maelle.mohr@epitech.eu</a>
</section>

<style>
  .contact {
    max-width: 720px;
    margin: 0 auto;
    padding: 4rem 1.5rem 6rem;
    text-align: center;
  }
  .section-title {
    font-family: 'DM Serif Display', serif;
    font-size: 1.75rem;
    color: var(--ink);
    margin-bottom: 1rem;
  }
  .contact-text {
    font-family: 'DM Sans', sans-serif;
    font-size: 1rem;
    color: var(--muted);
    margin-bottom: 1.5rem;
  }
  .contact-email {
    font-family: 'DM Mono', monospace;
    font-size: 1.1rem;
    color: var(--accent);
    text-decoration: none;
    border-bottom: 1px solid var(--accent);
    padding-bottom: 2px;
    transition: opacity 0.2s ease;
  }
  .contact-email:hover {
    opacity: 0.75;
  }
</style>
